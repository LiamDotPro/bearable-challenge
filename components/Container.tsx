import styled from 'styled-components/native'
import {color, space, flexbox, border, layout, position} from 'styled-system'

const Container = styled.View`
  ${color}
  ${space}
  ${flexbox}
  ${border}
  ${layout}
  ${position}
`

export default Container